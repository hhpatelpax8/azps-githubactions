name: Azure PowerShell Deployment

on:
  push:
    branches:
      - main  
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: windows-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Clear PowerShell Session
      run: |
        Get-Module -ListAvailable | Remove-Module -Force

    - name: Install Azure PowerShell
      run: |
        Install-Module -Name Az -Force -AllowClobber
        Import-Module -Name Az
 
    - name: Set up Azure PowerShell
      uses: azure/login@v1
      with:
        creds: |
          ${{ secrets.AZURE_CREDENTIALS }}

    - name: Run Azure Deployment Script
      run: |
        # Authenticate with Azure
        Connect-AzAccount
        
        # Run the masterscript.ps1 file
        .\masterscript.ps1

    - name: Logout Azure PowerShell
      run: |
        az logout
